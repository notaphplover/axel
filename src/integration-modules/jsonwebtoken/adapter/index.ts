import { jsonwebtokenContainer } from './config/container';

// eslint-disable-next-line @typescript-eslint/typedef
export const jsonwebtokenAdapter = {
  config: {
    container: jsonwebtokenContainer,
  },
};
