import { JWT_DOMAIN_TYPES } from './config/types';
import { JwtManager } from './JwtManager';

export { JwtManager };

// eslint-disable-next-line @typescript-eslint/typedef
export const jwtDomain = {
  types: JWT_DOMAIN_TYPES,
};
